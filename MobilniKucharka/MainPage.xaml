<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:MobilniKucharka.Classes"
             x:Class="MobilniKucharka.MainPage"
             Title="Co budeme vařit?">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="⚙️" Clicked="OnSettingsClicked" />
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- SEKCE 1: Typy a živiny -->
            <Label Text="1. Co hledáš za živiny/kategorie?" FontSize="18" FontAttributes="Bold" />
            <CollectionView x:Name="CategoriesList" SelectionMode="Multiple" HeightRequest="50">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Horizontal" ItemSpacing="10" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:FilterItem">
                        <Border Padding="10,5" StrokeShape="RoundRectangle 20" Stroke="Gray">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal">
                                        <VisualState.Setters>
                                            <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=Transparent, Dark=Transparent}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Selected">
                                        <VisualState.Setters>
                                            <Setter Property="BackgroundColor" Value="#4CAF50" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Label Text="{Binding Name}" VerticalOptions="Center" />
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- SEKCE 2: Co máš doma -->
            <Label Text="2. Co máš doma za suroviny?" FontSize="18" FontAttributes="Bold" />
            <CollectionView x:Name="IngredientsList" SelectionMode="Multiple" HeightRequest="50">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Horizontal" ItemSpacing="10" />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:FilterItem">
                        <Border Padding="10,5" StrokeShape="RoundRectangle 20" Stroke="Gray">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal">
                                        <VisualState.Setters>
                                            <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=Transparent, Dark=Transparent}" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Selected">
                                        <VisualState.Setters>
                                            <Setter Property="BackgroundColor" Value="#2196F3" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Label Text="{Binding Name}" VerticalOptions="Center" />
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Button Text="Najít recepty" 
                    Clicked="OnSearchClicked" 
                    BackgroundColor="#FF9800" 
                    TextColor="White" 
                    FontAttributes="Bold" 
                    CornerRadius="10"/>

            <!-- VÝSLEDKY: Seznam receptů -->
            <Label Text="Nalezené recepty:" FontSize="18" FontAttributes="Bold" Margin="0,10,0,0" />
            <CollectionView x:Name="RecipesList" SelectionMode="Single" SelectionChanged="OnRecipeSelected">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Recipe">
                        <Grid Padding="0,5">
                            <Border StrokeShape="RoundRectangle 10" StrokeThickness="1" Stroke="#DDDDDD" Padding="10">
                                <HorizontalStackLayout Spacing="15">
                                    <Image Source="{Binding ImageUrl}" WidthRequest="80" HeightRequest="80" Aspect="AspectFill" />
                                    <VerticalStackLayout VerticalOptions="Center">
                                        <Label Text="{Binding Name}" FontSize="16" FontAttributes="Bold" />
                                        <Label Text="{Binding Protein, StringFormat='Bílkoviny: {0}g'}" FontSize="12" TextColor="Gray" />
                                    </VerticalStackLayout>
                                </HorizontalStackLayout>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>